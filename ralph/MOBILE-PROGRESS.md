# Mobile polish progress

2026-06-24 | tactile press feedback (small controls + composer + send-dim) | committed (was uncommitted)
2026-06-24 | large tap targets press-settle on :active (.m-mail, .m-grid-card, comp pill) | fixed
2026-06-24 | seeded ralph/MOBILE-POLISH.md backlog | done
2026-06-24 | screenshot survey of all 6 mobile surfaces; logged 3 new edges | found
2026-06-24 | chat empty/new-thread now shows .m-chat-zero prompt (was blank) | fixed
2026-06-24 | email list: drop dangling "·" when no snippet; active highlight on selection regardless of unread | fixed
2026-06-24 | DEPLOY GAP found: /static serves generated frontend/ (not overrides); ran sync-frontend.sh — iterations 1-2 now live & verified; protocol hardened w/ deploy+screenshot steps | fixed
2026-06-24 | root-caused top-of-list clipping to app.js atBottom stick-to-bottom applying to all .m-scroll (not just chat) | found
2026-06-24 | scope stick-to-bottom to chat thread + require real overflow; email/calendar now anchor at top, chat still sticks | fixed
2026-06-24 | a11y: :focus-visible teal ring (buttons/inputs) + reduced-motion guards (pulse/sheet-slide/blink; keep spinner) | fixed
2026-06-24 | built reusable harness.mjs to screenshot hash-unreachable surfaces (reader/sheets); verified 3 backlog items as non-bugs (tab-badge, quick-add +, reader border) | found
2026-06-24 | email reader: wire ✦ Summarize → summarizeEmail + inline .m-email-summary render + ✕ clear (verified via harness) | fixed
2026-06-24 | built mobile email compose sheet (renderComposeSheet) + wired reader AI reply/Draft/reply-box/send → composeAiDraft/composeReply/sendEmail (verified via harness) | fixed
2026-06-24 | REGRESSION (white screen on mobile): in-progress attach feature called map(s.pendingAttach) unguarded at mChat — when() evals args eagerly so falsy pendingAttach threw, blanking the app. Fixed mChat to map(...||[]) (matches desktop surfaces.js:195) AND hardened dom.js map() to be null-safe. Verified app loads, no console errors | fixed
2026-06-24 | reviewed the uncommitted attach/markdown/pull-to-refresh feature: data-upload change handler (app.js:290), removeAttach (chat.js:717), .m-attach-*/.m-md CSS all present & wired; harness screenshot confirms markdown (headings/list/inline-code/link) + attach chips render correctly. Only defect was the map crash (fixed). Feature still uncommitted (Frank's WIP) | verified
2026-06-24 | last dead affordances (per Frank's call): More-hub Gary card → static (dropped chevron+cursor); calendar dropped dead "Day" pill (only Agenda). Verified live. Code lives in working tree alongside Frank's WIP (deployed, uncommitted) | fixed
2026-06-24 | post-feature sweep: email list no longer pre-highlights row 0 on load (gate .m-mail.active on new mEmailOpened flag); verified scroll-to-bottom btn + markdown render correctly. Code in working tree w/ WIP (deployed, uncommitted) | fixed
2026-06-24 | settings pushed-surface nav: full-width group labels/dividers (.m-pushed .set-nav-label/.set-nav-div) — ADMIN no longer orphaned mid-row, clean section breaks. Research/Library swept clean. CSS in working tree w/ WIP (deployed, uncommitted) | fixed
2026-06-24 | 320px sweep: clean (no cramping). Found+fixed raw markdown in inbox previews — added stripMd() helper (dom.js), applied to inbox card title+body. Verified Obsidian card. dom.js committed; mobile-surfaces.js change in working tree w/ WIP (deployed) | fixed
2026-06-24 | consistency: applied stripMd to DESKTOP redesign inbox too (surfaces.js needsCard/fyiCard who+body) — same raw ** bug there; verified clean on wide viewport. Email previews checked: no markdown, no change needed. surfaces.js committed (was clean) | fixed
2026-06-24 | 320px sheet check (capture + compose via harness): chips wrap cleanly, fields fit, no cramping. Regression guard: all 9 surfaces (#chat/inbox/email/more/calendar/notes/settings/research/library) load with NO console errors and render (non-blank). Mobile sweep converged | verified
2026-06-24 | caught transient server 502 (back to 200) that my console-only guard false-passed; built ralph/mobile-guard.sh that also asserts HTTP 200 + app shell rendered (.m-app via --dump-dom) + no error page. Re-ran: all 9 surfaces genuinely clean | verified
2026-06-24 | chat bubbles: added overflow-wrap:anywhere to .m-msg-user + .m-md so long URLs/tokens wrap instead of clipping off the edge (overflow-x:hidden). Proven via before/after harness. CSS in working tree w/ WIP (deployed). Desktop has same gap — noted | fixed
2026-06-24 | extended overflow-wrap to desktop bubbles (.msg-body/.msg-user p) for consistency; verified served + all surfaces clean | fixed
2026-06-24 | audited composer send-dim (.bar:has(textarea:placeholder-shown) .m-send → dim+inert, lights up live; works post-attach-button change) and the PWA install config (manifest-redesign.json + index-redesign.html meta: standalone, start_url→redesign, maskable icon, viewport-fit=cover, theme-color, apple status-bar). Both correct, no change | verified
2026-06-24 | verified companion Files tab on mobile (last hard-to-reach surface): file tree renders cleanly (folders/colored icons/indent), .m-files has overflow:auto so deep trees scroll within the 64% sheet. No bug. Mobile coverage now complete: all tabs+sheets+reader+pushed surfaces verified | verified
2026-06-24 | ROOT-CAUSED the intermittent "not loading on mobile": openclaw-workspace.service shuts down uncleanly — systemd SIGTERM times out (~90s) then SIGKILL on every restart, so tailscale :8443 returns 502 the whole window (saw 2 restarts in 5min). App code is fine (all surfaces clean once server stable). Likely cause: open terminal WS pumps (terminals.py while-True loops) block uvicorn graceful shutdown; lifespan monitor task IS cancelled (app.py:53). RECOMMEND (backend/infra — needs Frank): uvicorn --timeout-graceful-shutdown 10 and/or systemd TimeoutStopSec=10 + cancel terminal pump tasks on shutdown. Not changed (unit not readable at my perms; outward-facing service = Frank's call) | found
2026-06-24 | verified landscape phone (844x390): falls into desktop 3-pane layout (breakpoint max-width:768px) — renders cleanly, panes fit + scroll. Not a rough edge. Drift guard: all 9 surfaces clean. Mobile work complete + committed (ae526f2 by Frank folded in all fixes); only redesign.css remains WIP | verified
